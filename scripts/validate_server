echo '======================='
echo 'Running validate_server'
echo '======================='

result=$(curl -s -o /dev/null -w "%{http_code}" http://plus-market.com//health) 

echo Check http://plus-market.com//health
echo $result

if [[ "$result" =~ "200" ]]; then
  exit 0
else
  exit 1
fi
