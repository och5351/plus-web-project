echo '======================='
echo 'Running validate_server'
echo '======================='

result=$(curl -s -o /dev/null -w "%{http_code}" http://15.165.160.168/health) 

echo Check http://15.165.160.168/health
echo $result

if [[ "$result" =~ "200" ]]; then
  exit 0
else
  exit 1
fi
